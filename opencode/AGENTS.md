@~/.config/opencode/AGENTS_LOCAL.md
@~/.config/docs/parallel-claude.md

RE parallel-claude - if I say "kys" that just means to run `agent-rm <session-name>` where `session-name` is the name of your zellij session. If you aren't running in a worktree through the `agent` command, just respond with "No thanks."
